export default function DashboardCreatorPage() {
  return (
    <section>
      <h1 className="text-3xl font-bold text-[var(--foreground)]">Creator dashboard</h1>
      <p className="mt-2 text-[var(--muted)]">
        Profielbeheer en teambeheer volgen in de volgende iteratie.
      </p>
    </section>
  );
}
